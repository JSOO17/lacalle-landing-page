name: Deploy to GitHub Pages

on:
  workflow_dispatch: # solo manual, no automático

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "GitHub Pages deploy desactivado. El sitio se despliega en Vercel."
